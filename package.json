{
  "name": "@marscollective/gatsby-starter-portfolio",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "GATSBY_EXPERIMENTAL_PAGE_BUILD_ON_DATA_CHANGES=true gatsby build --log-pages",
    "dev": "gatsby develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "yarn build && netlify deploy -p"
  },
  "dependencies": {
    "@marscollective/gatsby-theme-portfolio": "*",
    "gatsby": "^2.28.2",
    "react": "^16.14.0",
    "react-dom": "^16.14.0"
  }
}
